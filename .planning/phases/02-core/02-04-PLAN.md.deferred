---
phase: 02-core
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/ocr_service.py
  - app/blueprints/certificates/routes.py
  - app/blueprints/certificates/__init__.py
  - app/templates/certificates/ocr_confirm.html
  - app/templates/certificates/ocr_upload.html
  - app/config.py
autonomous: false
requirements:
  - OCR-01

must_haves:
  truths:
    - "User can upload certificate image/PDF and system auto-extracts text using local OCR"
    - "User sees OCR results in a confirmation page with editable fields"
    - "User can correct OCR errors before saving"
    - "User confirms and certificate is saved directly without admin review"
    - "OCR auto-detects certificate type (4 standard types or special)"
  artifacts:
    - path: "app/services/ocr_service.py"
      provides: "PaddleOCR integration with type detection and field extraction"
      exports: "OCRService.recognize_certificate(), OCRService.detect_type()"
    - path: "app/blueprints/certificates/routes.py"
      provides: "OCR upload route and confirmation route"
      exports: "/certificates/ocr/upload, /certificates/ocr/confirm"
    - path: "app/templates/certificates/ocr_upload.html"
      provides: "Drag-and-drop upload interface"
    - path: "app/templates/certificates/ocr_confirm.html"
      provides: "OCR results confirmation with editable fields"
  key_links:
    - from: "app/templates/certificates/ocr_upload.html"
      to: "/certificates/ocr/upload"
      via: "fetch() API call"
      pattern: "fetch.*ocr/upload"
    - from: "app/templates/certificates/ocr_confirm.html"
      to: "/certificates/ocr/confirm"
      via: "form POST"
      pattern: "POST.*ocr/confirm"
---

<objective>
Add OCR certificate entry as an additional entry method alongside existing manual upload. Users upload a certificate image/PDF, the system automatically recognizes text using local PaddleOCR, extracts certificate type and fields, presents results for user confirmation, then saves directly to database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
**User Decisions (from CONTEXT.md - Certificate Entry Flow):**
- OCR replaces manual entry as the primary method
- Two entry methods: upload portal + directory monitoring (两种方式并存)
- OCR flow: upload → auto-recognize (type + fields) → user confirm → direct save
- Mixed field modes: Standard (4 types) + Special (free-form)
- Local OCR (PaddleOCR) - no cloud services, data privacy

**Existing Phase 2 Files:**
- `app/models/certificate.py` - Certificate and CertificateType models with JSONB fields
- `app/blueprints/certificates/routes.py` - Existing CRUD routes
- `app/blueprints/certificates/forms.py` - FIELD_SCHEMAS for 4 certificate types
- `app/blueprints/certificates/services.py` - CertificateService, ExcelImportService
- `app/templates/certificates/upload.html` - Existing manual upload form (KEEP)
- `app/services/file_storage_service.py` - Existing file storage service

**OCR Type Detection Keywords:**
| Certificate Type | Detection Keywords |
|------------------|-------------------|
| 比赛获奖证书 | 竞赛, 比赛, 获奖, 一等奖, 二等奖, 三等奖, 优秀奖, 冠军, 名次 |
| 荣誉证书 | 荣誉, 授予, 称号, 荣誉称号 |
| 资格证 | 资格证书, 职业资格, 证书编号, 有效期 |
| 职业技能等级证书 | 职业技能, 等级证书, 初级工, 中级工, 高级工, 技师, 高级技师 |

<interfaces>
<!-- Key types and contracts for OCR implementation -->

From app/models/certificate.py:
```python
class CertificateType(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(50), unique=True, nullable=False)
    fields_schema = db.Column(db.JSON)

class Certificate(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)
    certificate_type_id = db.Column(db.Integer, db.ForeignKey('certificate_types.id'), nullable=False)
    title = db.Column(db.String(200), nullable=False)
    file_path = db.Column(db.String(500), nullable=False)
    original_filename = db.Column(db.String(255))
    file_size = db.Column(db.Integer)
    file_mime_type = db.Column(db.String(100))
    fields = db.Column(db.JSON)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

From app/blueprints/certificates/forms.py:
```python
FIELD_SCHEMAS = {
    '比赛获奖证书': [
        {'name': 'competition_name', 'label': '比赛名称', 'type': 'string', 'required': True},
        {'name': 'award_level', 'label': '获奖等级', 'type': 'select', 'options': ['一等奖', '二等奖', '三等奖', '优秀奖'], 'required': True},
        {'name': 'award_date', 'label': '获奖日期', 'type': 'date', 'required': True},
        {'name': 'organizer', 'label': '主办单位', 'type': 'string', 'required': True},
        {'name': 'certificate_number', 'label': '证书编号', 'type': 'string', 'required': False},
    ],
    # ... other types
}
```

From app/services/file_storage_service.py:
```python
class FileStorageService:
    @staticmethod
    def save_file(file) -> dict:
        # Returns {'path': str, 'size': int}
```
</interfaces>

<user_setup>
- service: paddleocr
  why: "Local OCR engine for certificate recognition"
  env_vars:
    - name: PADDLEOCR_DEPS_INSTALLED
      source: "pip install paddlepaddle paddleocr - will be done in task"
  dashboard_config:
    - task: "No external dashboard needed - local deployment"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Create OCR Service with PaddleOCR</name>
  <files>app/services/ocr_service.py</files>
  <read_first>
    - app/services/file_storage_service.py (reference for service pattern)
    - app/blueprints/certificates/forms.py (FIELD_SCHEMAS for field extraction)
    - app/models/certificate.py (Certificate model structure)
  </read_first>
  <action>
    Create `app/services/ocr_service.py` with OCRService class:

    ```python
    """OCR Service using PaddleOCR for local certificate recognition."""
    import os
    import re
    from datetime import datetime
    from typing import Optional
    from paddleocr import PaddleOCR
    from PIL import Image
    import pdf2image

    # Initialize PaddleOCR (Chinese + English, use_angle_cls=True for text orientation)
    # OCR results cached in class to avoid re-initialization
    _ocr_engine = None

    def get_ocr_engine():
        global _ocr_engine
        if _ocr_engine is None:
            _ocr_engine = PaddleOCR(use_angle_cls=True, lang='ch', use_gpu=False, show_log=False)
        return _ocr_engine

    # Type detection keywords
    TYPE_KEYWORDS = {
        '比赛获奖证书': ['竞赛', '比赛', '获奖', '一等奖', '二等奖', '三等奖', '优秀奖', '冠军', '名次', '获奖证书'],
        '荣誉证书': ['荣誉', '授予', '称号', '荣誉称号', '授予单位'],
        '资格证': ['资格证书', '职业资格', '证书编号', '有效期', '资格'],
        '职业技能等级证书': ['职业技能', '等级证书', '初级工', '中级工', '高级工', '技师', '高级技师'],
    }

    class OCRService:
        """Service for OCR-based certificate recognition."""

        @classmethod
        def recognize_certificate(cls, file_path: str, file_mime_type: str) -> dict:
            """Recognize certificate from image or PDF file.

            Args:
                file_path: Path to the certificate file
                file_mime_type: MIME type (image/png, image/jpeg, application/pdf)

            Returns:
                dict with keys: success (bool), text (str), type (str), fields (dict), error (str)
            """
            try:
                # Convert PDF to images if needed
                if file_mime_type == 'application/pdf':
                    images = pdf2image.convert_from_path(file_path, dpi=200)
                    if not images:
                        return {'success': False, 'error': 'PDF转换失败 / PDF conversion failed'}
                    # Use first page
                    image_path = file_path + '.tmp.png'
                    images[0].save(image_path, 'PNG')
                    file_path = image_path

                # Run OCR
                ocr = get_ocr_engine()
                result = ocr.ocr(file_path, cls=True)

                # Clean up temp file
                if file_path.endswith('.tmp.png'):
                    os.remove(file_path)

                if not result or not result[0]:
                    return {'success': False, 'error': '未能识别出文字 / No text recognized'}

                # Extract all text lines
                text_lines = []
                for line in result[0]:
                    if line and len(line) >= 2:
                        text_lines.append(line[1][0])

                full_text = '\n'.join(text_lines)

                # Detect certificate type
                cert_type = cls.detect_type(full_text)

                # Extract fields based on type
                fields = cls.extract_fields(full_text, cert_type)

                # Generate title from first significant line
                title = cls.extract_title(text_lines, cert_type)

                return {
                    'success': True,
                    'text': full_text,
                    'type': cert_type,
                    'fields': fields,
                    'title': title,
                }

            except Exception as e:
                return {'success': False, 'error': f'OCR识别失败 / OCR recognition failed: {str(e)}'}

        @classmethod
        def detect_type(cls, text: str) -> str:
            """Auto-detect certificate type based on text content.

            Returns one of: 比赛获奖证书, 荣誉证书, 资格证, 职业技能等级证书, or '特殊证书' (special)
            """
            text_lower = text.lower()
            scores = {}

            for cert_type, keywords in TYPE_KEYWORDS.items():
                score = 0
                for keyword in keywords:
                    if keyword in text:
                        score += 1
                if score > 0:
                    scores[cert_type] = score

            if not scores:
                return '特殊证书'  # Special/unrecognized type

            # Return type with highest score
            return max(scores, key=scores.get)

        @classmethod
        def extract_fields(cls, text: str, cert_type: str) -> dict:
            """Extract dynamic fields based on certificate type.

            Returns dict of field_name -> value mappings.
            """
            fields = {}

            if cert_type == '比赛获奖证书':
                # Extract competition name (usually first major line)
                fields['competition_name'] = cls.extract_field(text, ['比赛名称', '竞赛名称', '赛事名称'])
                # Extract award level
                fields['award_level'] = cls.extract_field(text, ['一等奖', '二等奖', '三等奖', '优秀奖', '冠军', '亚军', '季军'])
                # Extract date
                fields['award_date'] = cls.extract_date(text)
                # Extract organizer
                fields['organizer'] = cls.extract_field(text, ['主办单位', '组织单位', '主办方', '组织方'])
                # Extract certificate number
                fields['certificate_number'] = cls.extract_field(text, ['证书编号', '编号', '证书号'])

            elif cert_type == '荣誉证书':
                fields['honor_title'] = cls.extract_field(text, ['荣誉名称', '称号'])
                fields['grant_date'] = cls.extract_date(text)
                fields['grantor'] = cls.extract_field(text, ['授予单位', '授予机构'])
                fields['reason'] = cls.extract_field(text, ['获得原因', '获奖原因'])

            elif cert_type == '资格证':
                fields['certificate_name'] = cls.extract_field(text, ['证书名称', '资格名称'])
                fields['certificate_number'] = cls.extract_field(text, ['证书编号', '编号'])
                fields['issue_date'] = cls.extract_date(text)
                fields['expiry_date'] = cls.extract_field(text, ['有效期至', '有效期', '过期日期'])
                fields['issuing_authority'] = cls.extract_field(text, ['发证机构', '颁发机构', '发证单位'])

            elif cert_type == '职业技能等级证书':
                fields['skill_name'] = cls.extract_field(text, ['职业技能', '职业名称'])
                fields['skill_level'] = cls.extract_field(text, ['五级/初级工', '四级/中级工', '三级/高级工', '二级/技师', '一级/高级技师'])
                fields['certificate_number'] = cls.extract_field(text, ['证书编号', '编号'])
                fields['issue_date'] = cls.extract_date(text)
                fields['issuing_authority'] = cls.extract_field(text, ['发证机构', '颁发机构', '发证单位'])

            # Remove None values
            return {k: v for k, v in fields.items() if v}

        @classmethod
        def extract_field(cls, text: str, keywords: list) -> Optional[str]:
            """Extract field value by finding keyword and returning subsequent text."""
            for keyword in keywords:
                idx = text.find(keyword)
                if idx != -1:
                    # Get text after keyword (up to next newline or 50 chars)
                    end_idx = min(idx + len(keyword) + 50, len(text))
                    value_text = text[idx + len(keyword):end_idx]
                    # Clean up: remove common separators
                    value_text = re.sub(r'^[\s:：\-\–\—]+', '', value_text)
                    value_text = value_text.strip()
                    # Take only first line
                    value_text = value_text.split('\n')[0].strip()
                    if value_text:
                        return value_text[:100]  # Limit length
            return None

        @classmethod
        def extract_date(cls, text: str) -> Optional[str]:
            """Extract date in various formats (YYYY-MM-DD, YYYY年MM月DD日, etc.)."""
            # Match date patterns
            patterns = [
                r'(\d{4})[年\-](\d{1,2})[月\-](\d{1,2})日?',
                r'(\d{4})[/年](\d{1,2})[/月](\d{1,2})',
            ]
            for pattern in patterns:
                match = re.search(pattern, text)
                if match:
                    year, month, day = match.groups()
                    return f'{int(year)}-{int(month):02d}-{int(day):02d}'
            return None

        @classmethod
        def extract_title(cls, text_lines: list, cert_type: str) -> str:
            """Extract certificate title from text lines.

            Heuristic: first line that is 5-50 chars and contains meaningful text.
            """
            for line in text_lines[:5]:  # Check first 5 lines
                line = line.strip()
                if 5 <= len(line) <= 50 and not line.isdigit():
                    return line
            # Fallback: first 30 chars of text
            return text_lines[0][:30] if text_lines else f'{cert_type} / {cert_type}'
    ```

    **DEPENDENCY NOTE:** This task requires installing PaddlePaddle and PaddleOCR. Add to requirements.txt:
    ```
    paddlepaddle>=3.0.0
    paddleocr>=2.7.0
    pdf2image>=1.17.0
    ```

    The executor should run:
    ```bash
    pip install paddlepaddle paddleocr pdf2image
    ```
  </action>
  <verify>
    <automated>grep -n "class OCRService" app/services/ocr_service.py && grep -n "def recognize_certificate" app/services/ocr_service.py</automated>
  </verify>
  <done>
    OCRService class exists with recognize_certificate(), detect_type(), extract_fields() methods. PaddleOCR is installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OCR Routes and Templates</name>
  <files>
    - app/blueprints/certificates/routes.py
    - app/blueprints/certificates/__init__.py
    - app/templates/certificates/ocr_upload.html
    - app/templates/certificates/ocr_confirm.html
  </files>
  <read_first>
    - app/blueprints/certificates/routes.py (existing routes structure)
    - app/blueprints/certificates/forms.py (FIELD_SCHEMAS)
    - app/templates/certificates/upload.html (existing upload template for reference)
    - app/services/ocr_service.py (created in Task 1)
  </read_first>
  <action>
    **Step 2a: Update certificates __init__.py to register OCR blueprint**

    Add to `app/blueprints/certificates/__init__.py`:
    ```python
    from app.blueprints.certificates.routes import certificates_bp
    ```

    **Step 2b: Add OCR routes to routes.py**

    Add these routes to the existing certificates_bp:

    ```python
    @certificates_bp.route('/ocr/upload', methods=['GET', 'POST'])
    @login_required
    def ocr_upload():
        """OCR upload page - accepts certificate image/PDF, runs OCR, shows confirmation."""
        if request.method == 'GET':
            return render_template('certificates/ocr_upload.html')

        # Handle file upload
        if 'file' not in request.files:
            flash('请选择文件 / Please select a file', 'danger')
            return redirect(request.url)

        file = request.files['file']
        if file.filename == '':
            flash('请选择文件 / Please select a file', 'danger')
            return redirect(request.url)

        # Validate file type
        allowed = {'pdf', 'png', 'jpg', 'jpeg', 'gif'}
        ext = file.filename.rsplit('.', 1)[-1].lower() if '.' in file.filename else ''
        if ext not in allowed:
            flash('不支持的文件格式 / Unsupported file format', 'danger')
            return redirect(request.url)

        try:
            # Save file temporarily for OCR
            import uuid
            temp_filename = f'ocr_temp_{uuid.uuid4().hex}.{ext}'
            temp_path = os.path.join(current_app.config['UPLOAD_FOLDER'], temp_filename)
            file.save(temp_path)

            # Run OCR
            from app.services.ocr_service import OCRService
            result = OCRService.recognize_certificate(
                temp_path,
                file.content_type or f'application/{ext}'
            )

            # Clean up temp file
            if os.path.exists(temp_path):
                os.remove(temp_path)

            if not result['success']:
                flash(result.get('error', 'OCR识别失败 / OCR recognition failed'), 'danger')
                return redirect(request.url)

            # Store OCR result in session for confirmation
            import json
            session['ocr_result'] = {
                'type': result['type'],
                'title': result['title'],
                'fields': result['fields'],
                'file_mime_type': file.content_type,
                'filename': file.filename,
            }

            return render_template('certificates/ocr_confirm.html',
                                   ocr_result=result,
                                   filename=file.filename)

        except Exception as e:
            flash(f'处理失败 / Processing failed: {str(e)}', 'danger')
            return redirect(url_for('certificates.ocr_upload'))


    @certificates_bp.route('/ocr/confirm', methods=['POST'])
    @login_required
    def ocr_confirm():
        """Confirm OCR results and save certificate."""
        import json

        ocr_result = session.get('ocr_result')
        if not ocr_result:
            flash('会话过期，请重新上传 / Session expired, please upload again', 'warning')
            return redirect(url_for('certificates.ocr_upload'))

        try:
            # Get form data
            title = request.form.get('title')
            cert_type_name = request.form.get('certificate_type')
            dynamic_fields_json = request.form.get('dynamic_fields_json', '{}')
            dynamic_fields = json.loads(dynamic_fields_json) if dynamic_fields_json else {}

            # Find certificate type
            from app.models.certificate import CertificateType
            cert_type = CertificateType.query.filter_by(name=cert_type_name).first()
            if not cert_type:
                flash('未找到证书类型 / Certificate type not found', 'danger')
                return redirect(url_for('certificates.ocr_upload'))

            # Note: File handling is different for OCR - user uploads file in ocr_upload
            # For now, create certificate without file (or store original filename only)
            # Future: could re-upload file after confirmation

            # Create certificate (no file for OCR flow - file_path will be empty)
            certificate = Certificate(
                user_id=current_user.id,
                title=title,
                certificate_type_id=cert_type.id,
                file_path='',  # OCR flow doesn't store file yet
                original_filename=ocr_result.get('filename', ''),
                file_mime_type=ocr_result.get('file_mime_type', ''),
                fields=dynamic_fields
            )
            db.session.add(certificate)
            db.session.commit()

            # Clear session
            session.pop('ocr_result', None)

            flash('证书创建成功 / Certificate created successfully', 'success')
            return redirect(url_for('certificates.detail', cert_id=certificate.id))

        except Exception as e:
            flash(f'创建失败 / Creation failed: {str(e)}', 'danger')
            return redirect(url_for('certificates.ocr_upload'))
    ```

    **Step 2c: Create ocr_upload.html template**

    Create `app/templates/certificates/ocr_upload.html`:
    ```html
    {% extends "base.html" %}

    {% block title %}OCR识别上传 - CertMgr{% endblock %}

    {% block content %}
    <h1>OCR识别上传 / OCR Recognition Upload</h1>
    <p class="text-muted">上传证书图片或PDF，系统自动识别内容 / Upload certificate image or PDF, system automatically recognizes content</p>

    <div class="card mb-4">
        <div class="card-body">
            <form method="POST" enctype="multipart/form-data" id="ocr-upload-form">
                <div class="mb-3">
                    <label class="form-label">证书文件 / Certificate File</label>
                    <input type="file" name="file" id="file-input" class="form-control"
                           accept=".pdf,.png,.jpg,.jpeg,.gif" required>
                    <small class="text-muted">支持 PDF、图片格式（PNG, JPG, GIF）</small>
                </div>

                <div id="preview-container" class="mb-3" style="display: none;">
                    <label class="form-label">预览 / Preview</label>
                    <img id="preview-image" class="img-thumbnail" style="max-height: 300px;">
                </div>

                <div class="mb-3">
                    <div class="progress" id="upload-progress" style="display: none;">
                        <div class="progress-bar" role="progressbar" style="width: 0%"></div>
                    </div>
                    <div id="ocr-status" class="text-muted" style="display: none;"></div>
                </div>

                <button type="submit" class="btn btn-primary" id="submit-btn">
                    上传并识别 / Upload and Recognize
                </button>
                <a href="{{ url_for('certificates.list') }}" class="btn btn-secondary">取消 / Cancel</a>
            </form>
        </div>
    </div>

    <div class="card">
        <div class="card-header">提示 / Tips</div>
        <div class="card-body">
            <ul class="mb-0">
                <li>确保证书图片清晰，光照均匀 / Ensure certificate image is clear with even lighting</li>
                <li>PDF文件只识别第一页 / PDF files only recognize the first page</li>
                <li>识别后请仔细核对信息是否正确 / Please carefully verify if the recognized information is correct</li>
            </ul>
        </div>
    </div>
    {% endblock %}

    {% block extra_js %}
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        const fileInput = document.getElementById('file-input');
        const previewContainer = document.getElementById('preview-container');
        const previewImage = document.getElementById('preview-image');
        const form = document.getElementById('ocr-upload-form');
        const submitBtn = document.getElementById('submit-btn');
        const progress = document.getElementById('upload-progress');
        const status = document.getElementById('ocr-status');

        // Preview image on select
        fileInput.addEventListener('change', function() {
            const file = this.files[0];
            if (file && file.type.startsWith('image/')) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    previewImage.src = e.target.result;
                    previewContainer.style.display = 'block';
                };
                reader.readAsDataURL(file);
            } else {
                previewContainer.style.display = 'none';
            }
        });

        // Show loading state on submit
        form.addEventListener('submit', function() {
            submitBtn.disabled = true;
            submitBtn.innerHTML = '识别中... / Recognizing...';
            progress.style.display = 'block';
            status.style.display = 'block';
            status.textContent = '正在识别证书内容... / Recognizing certificate content...';

            // Animate progress bar
            let width = 0;
            const interval = setInterval(function() {
                width += 10;
                if (width > 90) clearInterval(interval);
                progress.querySelector('.progress-bar').style.width = width + '%';
            }, 300);
        });
    });
    </script>
    {% endblock %}
    ```

    **Step 2d: Create ocr_confirm.html template**

    Create `app/templates/certificates/ocr_confirm.html`:
    ```html
    {% extends "base.html" %}

    {% block title %}确认证书信息 - CertMgr{% endblock %}

    {% block content %}
    <h1>确认证书信息 / Confirm Certificate Information</h1>
    <p class="text-muted">请核对系统识别的信息，如有错误请修正 / Please verify the information recognized by the system, correct any errors</p>

    {% if ocr_result %}
    <form method="POST" action="{{ url_for('certificates.ocr_confirm') }}" id="ocr-confirm-form">
        <input type="hidden" name="csrf_token" value="{{ csrf_token() }}">

        <div class="card mb-3">
            <div class="card-header">识别结果 / Recognition Result</div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label class="form-label">证书类型 *</label>
                            <select name="certificate_type" id="certificate_type" class="form-select" required>
                                <option value="比赛获奖证书" {% if ocr_result.type == '比赛获奖证书' %}selected{% endif %}>比赛获奖证书</option>
                                <option value="荣誉证书" {% if ocr_result.type == '荣誉证书' %}selected{% endif %}>荣誉证书</option>
                                <option value="资格证" {% if ocr_result.type == '资格证' %}selected{% endif %}>资格证</option>
                                <option value="职业技能等级证书" {% if ocr_result.type == '职业技能等级证书' %}selected{% endif %}>职业技能等级证书</option>
                                <option value="特殊证书" {% if ocr_result.type == '特殊证书' %}selected{% endif %}>特殊证书</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">证书标题 *</label>
                            <input type="text" name="title" id="title" class="form-control" required
                                   value="{{ ocr_result.title or '' }}">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="alert alert-info">
                            <strong>文件名 / Filename:</strong> {{ filename }}<br>
                            <strong>识别类型 / Detected Type:</strong> {{ ocr_result.type }}<br>
                            <strong>可信度 / Confidence:</strong> {{ '高 / High' if ocr_result.fields else '低 / Low - please verify' }}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="card mb-3">
            <div class="card-header">动态字段 / Dynamic Fields</div>
            <div class="card-body">
                <div id="dynamic-fields-container">
                    <!-- Dynamic fields rendered by JavaScript based on selected type -->
                </div>
            </div>
        </div>

        <input type="hidden" name="dynamic_fields_json" id="dynamic_fields_json" value="{}">

        <div class="mb-3">
            <button type="submit" class="btn btn-success">确认保存 / Confirm and Save</button>
            <a href="{{ url_for('certificates.ocr_upload') }}" class="btn btn-secondary">重新上传 / Upload Again</a>
            <a href="{{ url_for('certificates.list') }}" class="btn btn-outline-secondary">取消 / Cancel</a>
        </div>
    </form>
    {% else %}
    <div class="alert alert-warning">
        无识别结果，请重新上传 / No recognition result, please upload again
    </div>
    <a href="{{ url_for('certificates.ocr_upload') }}" class="btn btn-primary">重新上传 / Upload Again</a>
    {% endif %}
    {% endblock %}

    {% block extra_js %}
    <script>
    const FIELD_SCHEMAS = {{ FIELD_SCHEMAS|tojson }};

    document.addEventListener('DOMContentLoaded', function() {
        updateDynamicFields();
        document.getElementById('certificate_type').addEventListener('change', updateDynamicFields);

        document.getElementById('ocr-confirm-form').addEventListener('submit', function() {
            // Collect dynamic fields before submit
            const fields = {};
            document.querySelectorAll('#dynamic-fields-container input, #dynamic-fields-container select, #dynamic-fields-container textarea').forEach(function(el) {
                if (el.name && el.value) {
                    fields[el.name.replace('dynamic_', '')] = el.value;
                }
            });
            document.getElementById('dynamic_fields_json').value = JSON.stringify(fields);
        });
    });

    function updateDynamicFields() {
        const typeSelect = document.getElementById('certificate_type');
        const container = document.getElementById('dynamic-fields-container');
        const selectedType = typeSelect.value;
        const schema = FIELD_SCHEMAS[selectedType] || [];

        container.innerHTML = '';

        if (schema.length === 0) {
            container.innerHTML = '<p class="text-muted">特殊证书类型，请手动填写所有字段 / Special certificate type, please fill in all fields manually</p>';
            return;
        }

        // Pre-fill from OCR result
        const ocrFields = {{ (ocr_result.fields if ocr_result else {})|tojson }};

        schema.forEach(function(field) {
            const div = document.createElement('div');
            div.className = 'mb-3';

            const existingValue = ocrFields[field.name] || '';

            let inputHtml = '';
            if (field.type === 'select') {
                inputHtml = `
                    <select class="form-select" name="dynamic_${field.name}" id="dynamic_${field.name}">
                        <option value="">请选择 / Select</option>
                        ${(field.options || []).map(function(opt) {
                            const selected = existingValue === opt ? 'selected' : '';
                            return `<option value="${opt}" ${selected}>${opt}</option>`;
                        }).join('')}
                    </select>`;
            } else if (field.type === 'text') {
                inputHtml = `<textarea class="form-control" name="dynamic_${field.name}" id="dynamic_${field.name}" rows="2">${existingValue}</textarea>`;
            } else if (field.type === 'date') {
                inputHtml = `<input type="date" class="form-control" name="dynamic_${field.name}" id="dynamic_${field.name}" value="${existingValue}">`;
            } else {
                inputHtml = `<input type="text" class="form-control" name="dynamic_${field.name}" id="dynamic_${field.name}" value="${existingValue}">`;
            }

            div.innerHTML = `
                <label class="form-label">${field.label}${field.required ? ' *' : ''}</label>
                ${inputHtml}`;
            container.appendChild(div);
        });
    }
    </script>
    {% endblock %}
    ```

    **Step 2e: Add navigation link to OCR upload**

    Update `app/templates/base.html` to add OCR upload link in navigation. Add after the existing certificates nav link:
    ```html
    <li class="nav-item">
        <a class="nav-link" href="{{ url_for('certificates.ocr_upload') }}">OCR识别 / OCR</a>
    </li>
    ```
  </action>
  <verify>
    <automated>grep -n "def ocr_upload" app/blueprints/certificates/routes.py && grep -n "def ocr_confirm" app/blueprints/certificates/routes.py && test -f app/templates/certificates/ocr_upload.html && test -f app/templates/certificates/ocr_confirm.html</automated>
  </verify>
  <done>
    OCR upload route at /certificates/ocr/upload renders confirmation page. OCR confirm route saves certificate. Both templates exist with proper field schemas.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify OCR Flow</name>
  <what-built>OCR certificate entry flow - upload page, OCR recognition, confirmation page, save</what-built>
  <how-to-verify>
    1. Start the Flask app: `python -m flask --app app run`
    2. Login as a test user
    3. Navigate to `/certificates/ocr/upload`
    4. Upload a certificate image (test with any image file)
    5. Verify:
       - Upload page displays with file input
       - After upload, confirmation page shows OCR results
       - Certificate type dropdown has all 5 options (4 standard + special)
       - Dynamic fields appear based on selected type
       - Form is editable (user can correct OCR errors)
       - Submit saves certificate and redirects to detail page
    6. Check certificate appears in list at `/certificates/`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
**OCR Recognition Quality Checks:**
- OCR correctly identifies Chinese text from certificate images
- Certificate type detection works for all 4 standard types
- Date extraction handles multiple formats (YYYY-MM-DD, YYYY年MM月DD日)
- Fallback to "特殊证书" when no type keywords detected

**Integration Checks:**
- OCR upload route accessible at /certificates/ocr/upload
- OCR confirmation route saves certificate to database
- Dynamic fields render correctly for each certificate type
- User can edit any field before confirmation
</verification>

<success_criteria>
1. User can access `/certificates/ocr/upload` and see OCR upload page
2. User can upload an image/PDF file
3. System runs PaddleOCR and returns recognition results
4. Confirmation page displays with editable fields
5. User can correct OCR errors
6. User confirms and certificate is saved with user_id=current_user.id
7. Certificate appears in user's certificate list
8. OCR does NOT require admin review - direct save per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/02-core/02-04-SUMMARY.md`
</output>
